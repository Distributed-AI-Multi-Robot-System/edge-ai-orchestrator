# STT Configuration
STT_DEVICE=cpu              # Options: cpu, cuda
STT_MODEL_BG=base           # Background model for accuracy (base, small, medium, large)
STT_MODEL_TAIL=tiny         # Tail model for low latency (tiny, base)

# TTS Configuration (placeholder)
# TTS_DEVICE=cpu

# Agent Configuration (placeholder)
# OPENAI_API_KEY=your_key_here
